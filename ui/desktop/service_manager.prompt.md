完成 `desktop/service_manager.py` 文件的编写，主要功能是管理和启动桌面应用的服务，包括：

1. 在启动 app.js 时，同步启动 `api/main.py` 中定义的 API 服务, 若服务已经在运行，则先关闭再重新启动
2. 在关闭 app.js 时，自动关闭 API 服务
3. 提供一个接口用于检查 API 服务的状态（是否在运行）

要求：

1. 关注点分离
2. 代码结构清晰，易于维护