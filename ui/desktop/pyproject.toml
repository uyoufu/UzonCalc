[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "uzoncalc-desktop"
version = "0.1.0"
description = "UzonCalc Desktop Application"
requires-python = ">=3.10"
dependencies = [
    "pywebview",
    "psutil",
]
