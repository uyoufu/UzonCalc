# Api development guide

## router

- use `oid` instead of `id` for resource identification in requests
- case style: camelCase for request/response fields, snake_case for others
- use RESTful API design principles for route naming and HTTP methods
- follow CRUD conventions for resource management
