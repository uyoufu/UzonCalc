[tool.uv.workspace]
# 根目录仅管理 workspace，避免根工具依赖进入核心包发布元数据。
members = ["src/core", "src/api"]

[dependency-groups]
# 根目录预览服务依赖，仅用于 dev_server.py。
preview = [
    "fastapi>=0.134.0",
    "uvicorn>=0.41.0",
]
# 根目录测试入口依赖，不参与 uzoncalc 发布。
dev = [
    "astpretty>=3.0.0",
    "pytest>=9.0.3",
]
